<div id="actions-select-group">
<br>

<label name="actions">Actions</label>
<select id="actions-select" name="actions" class="form-control">
  <% @hash[@type].each do |action|%>

    <option value="<%= action['value']%>"><%= action['name']%></option>

  <%end%>

</select>
</div>
